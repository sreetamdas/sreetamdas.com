---
title: "\n\n\nTanStack Start"
published: true
published_at: "2026-05-16"
---

import { MainTitle, StarHistory,Title } from "./-components";
import { Gradient } from "@/lib/components/Typography";
import {
  RouterStateDemo,
  RscDemo,
  CompositeSlotsDemo,
} from "./-demos";
import {
  UrlStateBuildUp,
  TypeSafetyBuildUp,
  RscBuildUp,
  CompositeBuildUp,
} from "./-code";

---

<div className="flex items-center justify-center pt-40">
  <h1 className="font-serif text-9xl font-bold text-balance whitespace-pre-line font-stretch-semi-condensed">Hello 👋</h1>
</div>

---

<MainTitle stage="title" />

---

# Sreetam Das

<img
  src="/SreetamDas.jpg"
  alt="Sreetam Das"
  className="mx-auto absolute top-28 right-28 mb-6 size-60 rounded-full border-4 border-white/10 object-cover"
/>

- Senior Frontend Engineer II
- Web Experience at Remote

---

# Remote

- Help companies hire, manage and pay people

<Steps>

- Full time employment, contractors, payroll
- No offices!
- Yes, I work remotely for Remote
- Yes, we have received questions about TV remote issues

</Steps>

<Notes>

Suppose your company wants to hire somone in Singapore,
it's pretty straightforward when they're a contractor, but for
FTEs, it gets complicated fast: payroll, benefits, tax deductions,
filing income tax, employments laws are different in every country,
Global employment is hard - Remote builds the indrastructure to do it right

we manage our own entities in different countries, allowing your company to hire the
right person, and take care of all the difficult stuff

</Notes>

---

# Engineering at Remote

- Since 2020: Growth Engineering for 4 years, now WebXP
- ~300 across engineering, ~50 teams across 40+ countries
- Elixir backend: 5 million+ lines of code
- Next.js SPA, TanStack Query, TanStack Table, and friends

---

<Title>Why?</Title>

---

# sreetamdas.com

<StarHistory />

---

# Over the years

<Steps>

- plain HTML + CSS
- React SPA
- Next.js (pages router)
- Next.js App Router + RSC
- Cloudflare / OpenNext
- **TanStack Start**

</Steps>

---

# the benchmark: Next.js

- RSC, first teased December 2020 — I was _hyped_
- Layouts: a genuinely great approach to share data and rendering
- No more API routes just to talk to the server

---

# But the model flipped

- server-first by default.
- `"use client"`
- serializable props
- Composition footgun
- Caching was suddenly a thing you had to think about
- The dev server _seemed_ super slow

---

# Looking around for options

---

# SvelteKit

- SvelteKit made Vite click
- data loading in nested routes

<img
  src="/images/slides/tanstack-start/sreetamandareena.webp"
  alt="Screenshot of sreetamandareena.com — SvelteKit site that made it click"
  className="mx-auto mt-6 rounded-lg border border-white/10 shadow-xl h-fit w-1/2"
/>

---

# Remix

<Tweet id="1813803190414110874" />

---

# Vite-pilled!

<Tweet id="1813803997876441264" />

---

# In summary

---

<Title>What?</Title>

---

# Server, opt-in

- full-stack on top of TanStack Router
- server when the route needs it
- client-first when it does not
- Vite, TypeScript, deploy anywhere

---

# Client-first, server-available

- SSR
- streaming
- server routes
- server functions
- middleware
- route-level caching

---

# Server functions / server routes

```tsx
const getStats = createServerFn({ method: "GET" })
  .validator(parseStatsInput)
  .handler(async ({ data }) => loadStats(data));
```

- functions: app-to-server RPC
- routes: public HTTP endpoints
- `GET` when the call should be cacheable

---

# Middleware across the boundary

```tsx
const boundaryMiddleware = createMiddleware({ type: "function" })
  .client(async ({ next }) => next({ context: { source: "browser" } }))
  .server(async ({ next }) => next({ context: { requestId: crypto.randomUUID() } }));
```

- `.client()` before the call leaves the browser
- `.server()` before the handler runs

---

# Loader data is cached

```tsx
createFileRoute("/blog/$slug")({
  loader: ({ params }) => getBlogRenderable({ data: params }),
  staleTime: 1000 * 60 * 60 * 24,
});
```

Caching moves back to the route.

---

# Rendering is a dial

```tsx
createFileRoute("/slides/tanstack-start/dev-lab")({
  ssr: true,
});
```

Full SSR, data-only, or client-only — per route.

---

# Streaming SSR

```tsx
loader: async ({ deps }) => ({
  shell: await getFastData(deps),
  slowPanel: getSlowData(deps),
});
```

Return an un-awaited promise.

---

# Build-time server functions

```tsx
export const getHighlightedCode = createServerFn({ method: "GET" })
  .middleware([staticFunctionMiddleware])
  .handler(async () => loadRwcCodeSamples());
```

Same primitive. Different clock.

---

# One route, many choices

```tsx
createFileRoute("/stats")({
  validateSearch,
  loaderDeps: ({ search }) => search,
  loader: ({ deps }) => getStats({ data: deps }),
  staleTime: 1000 * 60 * 5,
});
```

- typed URL state
- server function
- caching
- rendering choice

---

# I used to be anti-TypeScript

A confession, before I get to the type system.

<Steps>

- At React Nexus 2024, I was in a corridor debate with a bunch of engineers — some folks from Razorpay — about TypeScript
- The benefits are obvious: it catches bugs, it helps maintain code as it grows
- but on any moderately-sized project, the types become a whole dimension you wrangle continuously
- you know exactly what I mean if you've ever reached for `type-fest`
- …so I came around. Start's end-to-end types won me over — let me show you why

</Steps>

<Notes>

- Personal credibility beat — don't rush it. The "anti-TS" framing hooks anyone who's felt type-fest pain.
- The Razorpay corridor detail is real; name-drop only if it feels natural.
- This opens the "types" thread: the old-vs-new comparison, then URL state as typed state. The payoff is the "you never write `as` again" line.

</Notes>

---

# Type safety, the old way vs. Start

<TypeSafetyBuildUp />

<Steps>

- Next Pages: `FoobarPageQuery extends ParsedUrlQuery`, `params!`, `InferGetStaticPropsType` — just to give `params` a type
- Next App Router: every page redeclares `type PageParams = { params: Promise<...> }` and `await`s it — by hand, per route
- Start: one `validateSearch` schema; `useSearch()` returns `PlausibleDateRange` — no `as`, no `params!`

</Steps>

<Notes>

- The "before" code is real — pulled from this repo's git history. The `params!` non-null assertion had an eslint-disable next to it; that's how much we didn't want to write it.
- The App Router `PageParams` boilerplate is the worst part: a Promise you unwrap by hand, redeclared on every single page. No shared type — each route writes its own.
- If a Next person pushes back: "I can zod my searchParams too" — yes, every route, every time, and the cache still doesn't know your schema exists. Concede the parsing, hold the line on integration.

</Notes>

---

# URL state is not a string bag

<UrlStateBuildUp />

The URL is a state manager with a schema.

<Notes>

- Contrast: search params as a "string bag" everyone re-parses vs. a schema the route owns.
- `validateSearch` parses once; `loaderDeps` makes invalidation explicit, not vibes.

</Notes>

---

# Demo: `/stats`

<Steps>

- Change `?period=7d` to `?period=nonsense`
- The route keeps rendering with a safe default
- `Route.useSearch()` returns `PlausibleDateRange`, not "some string"
- `loaderDeps` makes cache invalidation explicit

</Steps>

<Notes>

- [DEMO] Pre-opened tabs: `/stats` and `?period=nonsense`. Click, don't type.
- Point: garbage → safe default; `useSearch()` returns a typed `PlausibleDateRange`.
- [FALLBACK] If the tab is stale, read it from `validateSearch` in the editor.

</Notes>

---

# Live: this deck's URL state

<RouterStateDemo />

<Notes>

- This is the deck itself: slide/step/live/presenter are validated search params.
- Walk a step — the values update live. No companion tab needed.

</Notes>

---

# RSC as an opt-in data primitive

<RscBuildUp />

Server-render the heavy content. Compose client islands where they matter.

RSC is still experimental in Start — I run it in production here anyway.

<Notes>

- On the blog, the loader server-renders the heavy MDX subtree with `renderServerComponent` and hands the result through loader data. It is _data_.
- This slide sets up composite components next — you need "RSC as data" before "RSC with slots" lands.

</Notes>

---

# This is not “RSC-first”

In Start, ordinary React components are interactive by default.

<Steps>

- Use RSC when it buys you something
- Cache it like route data
- Pass it through loader data
- Keep the client in charge of composition

</Steps>

<Notes>

- The mental model never inverts on you — which is exactly the thing that bugged me about server-first.

</Notes>

---

# Live: a server subtree as data

<RscDemo />

<Notes>

- [DEMO] The bordered panel was produced by `renderServerComponent` on the server; the counter beside it is ordinary client React.
- The client never imports the panel's code or its server-only inputs — only the rendered output.

</Notes>

---

# Composite components: slots

<CompositeBuildUp />

The server tree keeps its slots open. The client fills them with interactive islands — and the server passes typed data _out_.

<Notes>

- This is the clean inverse of the App Router footgun: there, a client import silently dragged the server subtree client-side. Here the server tree stays server-side and hands typed data out to the client island it wraps.
- Three slot types: `children` (no data), render props (server → client data), component props (reusable client components fed server data).
- Be honest about the edges: slots are opaque on the server (`React.Children.map`/`cloneElement` won't work on `props.children` — use render props), and render-prop args must be serializable (they cross React's Flight protocol).
- Still experimental into early v1; server-component serialization uses React Flight today (primitives, Dates, React elements).

</Notes>

---

# Live: composite slots

<CompositeSlotsDemo />

<Notes>

- [DEMO] The card is `createCompositeComponent` on the server. The "client island" box is a render-prop slot — it's client React, but the request id and render time inside it came _from the server_.
- The "Client clicks" button is the `children` slot: plain interactivity, no server data.
- [FALLBACK] If the composite round-trip fails (it's RC-stage RSC), fall back to the code build-up on the previous slide — the point is the shape, not the network.

</Notes>

---

# TanStack + Vite

The stack is composable, and the ecosystem is open.

<Steps>

- Router → Query → Table → Start — the same headline primitives, composing together
- The TanStack Query and Table I already use at work, plus TanStack DB
- Built on Vite: an open ecosystem, real plugins — not a bespoke bundler
- First-class TypeScript across all of it

</Steps>

<Notes>

- This ties back to the Remote slide: I didn't adopt Start cold — I'd been living in TanStack Query and Table at work.
- The point of the ecosystem beat: nothing here is a walled garden. Composable libraries, an open bundler, and types that hold across the seams.

</Notes>

---

# The dev loop, every day

I ship Next for a living. The thing I miss most is the feedback loop.

<Steps>

- Vite or Rsbuild: near-instant dev server startup
- HMR is near-instant — no Webpack/Turbopack wait
- Full-speed navigation in dev, not throttled
- Light on CPU and RAM on a laptop full of Chrome tabs

</Steps>

<Notes>

- Not a code feature — a productivity one. Say it as someone who ships Next daily.
- Keep it ~30s; it's a credibility beat, not a benchmark slide.

</Notes>

---

# Deployment is part of the design

This site runs on Cloudflare Workers.

<Steps>

- D1 for page views and likes
- KV for runtime storage
- Durable Objects for presence and slide sessions
- Smart Placement, logs, traces, version metadata
- Same route model, different runtime

</Steps>

<Notes>

- The deploy-anywhere pillar paying off: I didn't rewrite the app to move it to the edge.

</Notes>

---

# How to start

<Steps>

- TanStack CLI connects setup, docs, add-ons, and generated changes
- Use it to scaffold, search docs, and choose integrations
- Add a server function, a `validateSearch`, a loader — incrementally
- The mental model never inverts: you opt _into_ the server

</Steps>

<Notes>

- The "How" beat — one slide, ~40s. Keep it minimal: TanStack CLI is more than a template printer; it connects setup with docs/add-ons/integrations.
- If someone asks "migrate from Next?": route by route. Bring a page over, add a `validateSearch`, pick an `ssr` mode.
- Shoutouts to slip in near the close: Elixir (the backend that made me comfortable with a real type system) and Cloudflare (the runtime that made deploy-anywhere real).

</Notes>

---

# Finale: live slides are the demo

Everything you've watched is one TanStack Start route — including this live session.

<Steps>

- This deck's slide position is URL state; the live session is running right now
- Presenter controls viewer navigation
- Audience votes in polls
- Reactions go through hibernatable WebSockets — one session id maps to one Durable Object

</Steps>

<Notes>

- Self-contained close: introduce the live session here as the final proof — the website _is_ the demo.
- [DEMO] Fire a reaction from your phone so it lands on the projected deck.
- [FALLBACK] Show the poll results already collected — same DO round-trip.
- [CLOSE] Leave the deck up; take Q&A against the live session so the last thing they see is it working. Thank you.

</Notes>
